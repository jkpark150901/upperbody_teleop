# Combined install, for running both sides on one machine (e.g. the
# mock-backend loopback test in README.md). For the real two-PC deployment,
# install requirements-device.txt on the PN/SenseGlove PC and
# requirements-mujoco.txt on the MuJoCo PC instead -- the device PC doesn't
# need mujoco/glfw at all.
mujoco>=3.1.0
numpy>=1.24
pyyaml>=6.0
vedo>=2024.5.2
trimesh>=4.0     # robot_hand/mesh_loader.py -- vedo/VTK can't load the DG5F's .dae visual meshes directly
pycollada>=0.9   # trimesh's Collada (.dae) reader backend

# Optional Quest/OpenXR -> DG5F optimizer backend (--retargeter anydex /
# --hand-retargeter anydex). Kept in a separate file so MuJoCo-only installs
# can stay small.
-r requirements-anydex.txt
